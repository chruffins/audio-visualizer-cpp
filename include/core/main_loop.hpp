#pragma once

namespace core {

/**
 * Run the main application event loop.
 * Handles all UI rendering, event dispatching, and music playback.
 * Blocks until the user closes the application.
 */
void runMainLoop();

} // namespace core
